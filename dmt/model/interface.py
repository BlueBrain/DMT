"""An abstract class that will create an interface for a model."""
from abc import ABC, abstractmethod

class ModelInterface(ABC):
   """A metaclass that defines a class to interface with a model."""
   
